{% extends 'BaseBaseBundle::layout.html.twig' %}
{% block body %}
    <form method="post" action="" id="form-cadastro" class="form-horizontal tasi-form" novalidate="novalidate">
        <div class="row">
            <div class="col-lg-12">
                <section class="panel">
                    <header class="panel-heading">
                        Equipe Interna
                    </header>
                    <div class="panel-body">
                        <section class="panel">
                            <header class="panel-heading tab-bg-dark-navy-blue ">
                                <ul class="nav nav-tabs">
                                    <li class="active">
                                        <a data-toggle="tab" href="#plano" aria-expanded="true">Oi FIXO</a>
                                    </li>
                                    <li class="">
                                        <a data-toggle="tab" href="#pacote" aria-expanded="false">Oi TV</a>
                                    </li>
                                    <li class="">
                                        <a data-toggle="tab" href="#velocidade" aria-expanded="false">Velocidade</a>
                                    </li>
                                </ul>
                            </header>
                            <div class="panel-body">
                                <div class="tab-content">
                                    <div id="plano" class="tab-pane active">
                                        {% for plano in planos %}
                                            <div class="form-group">
                                                <label class="col-sm-4 control-label col-lg-4"
                                                       for="inputSuccess">{{ plano.getNoPlano() }}</label>

                                                <div class="input-group m-bot15 col-lg-2">
                                                    <span class="input-group-addon">$</span>
                                                    {{ formText('plano[' ~ plano.getIdPlano() ~ ']', '', {
                                                        class: 'form-control',
                                                        'data-mask': '99,99'
                                                    }) }}
                                                </div>
                                            </div>
                                        {% endfor %}
                                    </div>
                                    <div id="pacote" class="tab-pane">
                                        <div id="plano" class="tab-pane active">
                                            {% for pacote in pacotes %}
                                                <div class="form-group">
                                                    <label class="col-sm-4 control-label col-lg-4"
                                                           for="inputSuccess">{{ pacote.getNoPacote() }}</label>

                                                    <div class="input-group m-bot15 col-lg-2">
                                                        <span class="input-group-addon">$</span>
                                                        {{ formText('pacote[' ~ pacote.getIdPacote() ~ ']', '', {
                                                            class: 'form-control',
                                                            'data-mask': '99,99'
                                                        }) }}
                                                    </div>
                                                </div>
                                            {% endfor %}
                                        </div>
                                    </div>
                                    <div id="velocidade" class="tab-pane">
                                        <div id="plano" class="tab-pane active">
                                            {% for velocidade in velocidades %}
                                                <div class="form-group">
                                                    <label class="col-sm-4 control-label col-lg-4"
                                                           for="inputSuccess">{{ velocidade.getNoVelocidade() }}</label>

                                                    <div class="input-group m-bot15 col-lg-2">
                                                        <span class="input-group-addon">$</span>
                                                        {{ formText('velocidade[' ~ velocidade.getIdVelocidade() ~ ']', '', {
                                                            class: 'form-control',
                                                            'data-mask': '99,99'
                                                        }) }}
                                                    </div>
                                                </div>
                                            {% endfor %}
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </section>
                    </div>
                </section>
            </div>

            <div class="col-lg-12">
                <section class="panel">
                    <header class="panel-heading">
                        Equipe Externa
                    </header>
                    <div class="panel-body">
                        <section class="panel">
                            <header class="panel-heading tab-bg-dark-navy-blue ">
                                <ul class="nav nav-tabs">
                                    <li class="active">
                                        <a data-toggle="tab" href="#plano" aria-expanded="true">Oi FIXO</a>
                                    </li>
                                    <li class="">
                                        <a data-toggle="tab" href="#pacote" aria-expanded="false">Oi TV</a>
                                    </li>
                                    <li class="">
                                        <a data-toggle="tab" href="#velocidade" aria-expanded="false">Velocidade</a>
                                    </li>
                                </ul>
                            </header>
                            <div class="panel-body">
                                <div class="tab-content">
                                    <div id="plano" class="tab-pane active"a>
                                        {% for plano in planos %}
                                            <div class="form-group">
                                                <label class="col-sm-4 control-label col-lg-4"
                                                       for="inputSuccess">{{ plano.getNoPlano() }}</label>

                                                <div class="input-group m-bot15 col-lg-2">
                                                    <span class="input-group-addon">$</span>
                                                    {{ formText('plano[' ~ plano.getIdPlano() ~ ']', '', {
                                                        class: 'form-control',
                                                        'data-mask': '99,99'
                                                    }) }}
                                                </div>
                                            </div>
                                        {% endfor %}
                                    </div>
                                    <div id="pacote" class="tab-pane">
                                        <div id="plano" class="tab-pane active">
                                            {% for pacote in pacotes %}
                                                <div class="form-group">
                                                    <label class="col-sm-4 control-label col-lg-4"
                                                           for="inputSuccess">{{ pacote.getNoPacote() }}</label>

                                                    <div class="input-group m-bot15 col-lg-2">
                                                        <span class="input-group-addon">$</span>
                                                        {{ formText('pacote[' ~ pacote.getIdPacote() ~ ']', '', {
                                                            class: 'form-control',
                                                            'data-mask': '99,99'
                                                        }) }}
                                                    </div>
                                                </div>
                                            {% endfor %}
                                        </div>
                                    </div>
                                    <div id="velocidade" class="tab-pane">
                                        <div id="plano" class="tab-pane active">
                                            {% for velocidade in velocidades %}
                                                <div class="form-group">
                                                    <label class="col-sm-4 control-label col-lg-4"
                                                           for="inputSuccess">{{ velocidade.getNoVelocidade() }}</label>

                                                    <div class="input-group m-bot15 col-lg-2">
                                                        <span class="input-group-addon">$</span>
                                                        {{ formText('velocidade[' ~ velocidade.getIdVelocidade() ~ ']', '', {
                                                            class: 'form-control',
                                                            'data-mask': '99,99'
                                                        }) }}
                                                    </div>
                                                </div>
                                            {% endfor %}
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </section>
                    </div>
                </section>
            </div>
        </div>

        <div class="form-group">
            <div class="col-lg-offset-2 col-lg-10">
                <button type="submit" class="btn btn-primary">
                    <i class="fa fa-save"></i>
                    Salvar
                </button>

                <a href="/administrativo/perfil" class="btn btn-default">
                    <i class="fa fa-times"></i>
                    Cancelar
                </a>
            </div>
        </div>
    </form>

{% endblock %}